-- Run as the database owner after all migrations, including migration 013.
-- Replace invoice_power_bi_reader with a dedicated PostgreSQL role mapped to
-- the identity used by Power BI. Do not reuse the application runtime role.
GRANT CONNECT ON DATABASE invoice_processing TO invoice_power_bi_reader;
GRANT USAGE ON SCHEMA public TO invoice_power_bi_reader;
GRANT SELECT ON public.bi_invoice_metadata, public.bi_invoice_events,
    public.bi_invoice_corrections
    TO invoice_power_bi_reader;
